#include "main.h"
#include <stdio.h>
/**
* _strlen - integer is positive or negative
* @s: first integer
* Return: 0
*/

int _strlen(char *s)
{
	int i;
	int len = 0;

	if (*s == '\0')
	{
		return (len);
	}
}	else
	{
		for (i = 0; i < *s; i++)
		{
			*s[i];
		}
	}
	
