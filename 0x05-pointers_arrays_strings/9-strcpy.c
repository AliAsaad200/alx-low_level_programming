#include "main.h"
#include <stdio.h>
#include <string.h>
/**
* _strcpy - integer is positive or negative
* @src: first integer
* @dest: first integer
* Return: 0
*/
char *_strcpy(char *dest, char *src)
{
	char *result = strcpy(dest, src);

	return (result);
}
