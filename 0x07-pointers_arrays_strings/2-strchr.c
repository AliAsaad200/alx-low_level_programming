#include "main.h"
#include <stdio.h>
#include <string.h>
/**
* _strchr - integer is positive or negative
* @s: first integer
* @c: first integer
* Return: 0
*/

char *_strchr(char *s, char c)
{
	char *value = strchr(s, c);

	return (value);
}
