#include "main.h"
#include <stdio.h>
#include <string.h>
/**
* _strlen_recursion - integer is positive or negative
* @s: first integer
* Return: 0
*/
int _strlen_recursion(char *s)
{
	int len = strlen(s);

	return(len);
}
