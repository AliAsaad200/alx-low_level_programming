#include "main.h"
#include <stdio.h>
#include <stdlib.h>
/**
* alloc_grid - integer is positive or negative
* @width: first integer
* @height: first integer
* Return: 0
*/
nt **alloc_grid(int width, int height)
{
	if (width <= 0 || height <= 0)
	{
		return (NULL);
	}
}
