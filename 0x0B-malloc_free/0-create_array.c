#include "main.h"
#include <stdio.h>
#include <stdlib.h>
/**
* create_array - integer is positive or negative
* @size: first integer
* @c: first integer
* Return: 0
*/
char *create_array(unsigned int size, char c)
{
	if (size == 0)
	{
		return (NULL);

	}
	else
	{
		c = malloc(size);
	}
	free(size);
}
